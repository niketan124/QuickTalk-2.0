{
  "name": "chat-app-nik",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "build": "cd ../public/chat-app/ && npm run build",
    "install-public": "cd ../public/chat-app/ && npm install",
    "public": "cd ../public/chat-app/ && npm start",
    "start": "node index.js",
    "nodemon": "nodemon index.js",
    "heroku-postbuild": "npm run install-public && npm run build"
  },
  "author": "niketan",
  "license": "ISC",
  "dependencies": {
    "bcrypt": "^5.0.1",
    "cors": "^2.8.5",
    "dotenv": "^16.0.0",
    "express": "^4.17.3",
    "mongoose": "^6.2.10",
    "nodemon": "^2.0.15",
    "socket.io": "^4.4.1"
  }
}
